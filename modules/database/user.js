export default class User {
  constructor(chatId, firstName, lastName, preferences) {
    this.chatId = chatId;
    this.firstName = firstName;
    this.lastName = lastName;
    this.preferences = preferences;
  }
}
